# Check

A repo-wide check that can be run to ensure the current changes adhere to compiler and monorepo conventions.

# Usage

Run `bun check` at the monorepo root.

Use `required_permissions: ["all"]` to ensure success.
