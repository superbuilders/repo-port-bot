A pull request was merged in the source repository. Decide whether this change should be ported to the target repository.

Target repository path: `{{targetWorkingDirectory}}`

{{changedFiles}}

Inspect the target repository to determine whether equivalent files, modules, or APIs exist. Then return your decision and reason.
